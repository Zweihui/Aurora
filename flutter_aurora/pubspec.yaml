name: flutter_aurora
description: A new flutter module project.
version: 1.0.0+1

environment:
  sdk: ">=2.0.0-dev.68.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.2
  http: "^0.11.3+16"
  dio: ^1.0.1
  flutter_redux: ^0.5.2
  cached_network_image : ^0.4.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  json_serializable: ^1.2.1
  build_runner: ^0.9.0

flutter:
  uses-material-design: true
  module:
    androidPackage: com.example.flutteraurora
